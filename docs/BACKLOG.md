# Backlog — out of scope for v0, one line each

- Corrections: date picker to reach the full 7-day window (v0 shows today only).
- Auto-ended sessions: a "reviewed" acknowledgement for the Team Lead (v0 only shows the marker).
- Dark wall-screen variant of the live board (design brief, optional; tokens already in the handoff).
- Tile layouts 1b (wall-readable) and 1c (quiet) from the design as a Team Lead preference.
- Audit-log screen (v0 writes the log; nothing reads it in the UI).
- Sliding login sessions instead of the fixed 12-hour expiry.
- HTTPS on the LAN (self-signed certificate) so the login cookie can be `Secure`.
- Prisma enums on Postgres (v1) replacing the Zod-validated string columns.
- Vendored IBM Plex Sans Arabic so `npm run build` works offline.
